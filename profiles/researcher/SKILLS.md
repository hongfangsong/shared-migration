# SKILLS.md

###  互联网搜索
- 使用 DuckDuckGo 搜索工具查找最新信息。

### 🔍 搜索技能
- **工具使用**：熟练使用 `web_search` 工具。
- **关键词优化**：能够根据 Manager 的指令，自动拆解并优化搜索关键词。
- **信息筛选**：能够过滤广告和无关信息，只提取核心数据。

###  报告撰写
- 将搜索结果整理为结构化的 Markdown 文档。

###  文件写入
- **动态路径**：必须将报告保存到指定路径 `./shared/{TASK_ID}_research_report.md`。
- **任务ID遵循**：`{TASK_ID}` 由 Manager 在任务开始时指定，你必须严格遵循 Manager 的指令使用该 ID 作为文件名前缀。